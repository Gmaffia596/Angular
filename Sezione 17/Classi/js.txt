/* vecchio modo per js per definire una classe
function Animal(name) {
    this.name=name;
}

Animal.prototype.getName = function() {
    return this.name;
}
var cat = new Animal('Cat');
console.log(cat.getName());*/
